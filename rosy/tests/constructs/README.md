# Construct cases

One `.rosy` file per case. Runner: `rosy test` / `rosy test --filter pnpro` / `rosy test --bless`.

```
BEGIN;
    ...
END;

======= expect
<stdout>

======= fox
<optional COSY source>
```
