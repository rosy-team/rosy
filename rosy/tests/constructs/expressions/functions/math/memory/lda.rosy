BEGIN;
    VARIABLE (VE) V;
    VARIABLE (RE) N;
    V := 3 & 2;
    N := LDA(V);
    WRITE 6 N;
END;

======= expect
 10.00000000000000

======= fox
BEGIN;
PROCEDURE RUN;
    VARIABLE V 100;
    VARIABLE N 1;
    V := 3 & 2;
    N := LDA(V);
    WRITE 6 N;
ENDPROCEDURE;
RUN;
END;
