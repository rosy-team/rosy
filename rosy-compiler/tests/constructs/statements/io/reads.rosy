BEGIN;
    VARIABLE (ST) S;
    OPENF 10 'test_reads.txt' 'unknown';
    WRITE 10 'hello world';
    CLOSEF 10;
    OPENF 10 'test_reads.txt' 'old';
    READS 10 S;
    CLOSEF 10;
    WRITE 6 S;
END;

======= expect
hello world

======= fox
BEGIN;
  VARIABLE (ST) S;
  OPENF 10 'test_reads.txt' 'unknown';
  WRITE 10 'hello world';
  CLOSEF 10;
  OPENF 10 'test_reads.txt' 'old';
  READS 10 S;
  CLOSEF 10;
  WRITE 6 S;
END;
